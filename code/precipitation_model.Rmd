---
title: "precipitation"
output: html_document
date: "2024-05-09"
---

```{r}
library("tidymodels")
library("data.table")
library("report")
library("gridExtra")
library("extremogram")
library("doParallel")
source("utils.R")
source("metrics.R")


reanalysis <- fread('reanalysis_precipitation_training_data.csv')
cmip_6 <- fread('cmip6_precipitation_to_downscale_data.csv') 
```

```{r}
res_to_plot <- reanalysis |> inner_join(cmip_6, by = join_by(time)) |> rename("obs_pr_daily" = "pr_daily.x", "cmip_pr_daily" = "pr_daily.y")

ggplot(res_to_plot) +
  geom_histogram(aes(x=obs_pr_daily), alpha=.5) +
  geom_histogram(aes(x=cmip_pr_daily), fill = "red", alpha=.5)

rm(res_to_plot) 
```
